# Implementation Plan: Global Docs Sync (047)

1. **System Document Update**
   - Open `core-web/system.md`.
   - Replace ".NET 6" with ".NET 8 LTS".
   - Add "Tailwind CSS v4" to Frontend SPA stack.

2. **Frontend Audit Report Update**
   - Open `docs/audit/FRONTEND_AUDIT_REPORT.md`.
   - Modify the `Feed` and `SideBar` rows in the component table to status "Completado/Refactorizado".
   - Insert `UserProfile` row indicating completion.
   - Replace the legacy CSS grid diagnostics with a note that the CSS was purged and replaced with Tailwind CSS v4.

3. **Roadmap Update**
   - Open `docs/project_docs/ROADMAP.md`.
   - Mark T2.4 as completed via CV transplant.
   - Add a note about the successful implementation of Employer Passwordless and Community Moderation.
