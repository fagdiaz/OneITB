# Tasks: Header UX & CV Profile (022)

**Input**: Design documents from `/specs/022-header-ux-cv-profile/`

## Phase 1: Implementation
- [x] T001 Adjust "ONEITB" title in `Header.jsx` to navigate to `/social` using React Router `<Link>`.
- [x] T002 In `Nav.jsx`, configure "Inicio" link to point directly to `/social/feed`, and ensure other links are clean.
- [x] T003 Verify live updates in dual column layout inside `UserProfile.jsx`.
- [x] T004 Build validation check (`npm run build`).

