# Tasks: UI Polish and Assets Fix (015)

**Input**: Design documents from `/specs/015-ui-polish-assets/`

## Phase 1: Implementation
- [x] T001 Inject FontAwesome cdnjs Link in `index.html` and remove local css import in `main.jsx`.
- [x] T002 In `SideBar.jsx`, update the avatar img source to load dynamically from `https://ui-avatars.com/api/?name={name}`.
- [x] T003 Append refactored visual CSS properties for forms to `styles.css`.
- [x] T004 Build and run validation checks.
