# Feature Specification: Fix GraphQL Query Names

**Feature Branch**: `095-fix-graphql-query-names`

**Created**: 2026-06-13

**Status**: In Progress

## Requirements
- **FR-001**: Cambiar el campo de respuesta GraphQL en `subjects.js` de `getSubjects` a `subjects`.
- **FR-002**: Cambiar el campo de respuesta GraphQL en `inquiries.js` de `getInquiries` a `inquiries`.
- **FR-003**: Adaptar `Feed.jsx` para mapear desde `data?.subjects` y `data?.inquiries`.
