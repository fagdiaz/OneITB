# Feature Specification: Flat Mutation Alignment

**Feature Branch**: `091-build-flat-mutation`

**Created**: 2026-06-13

**Status**: In Progress

## Requirements
- **FR-001**: Recrear el formulario de publicación en `Feed.jsx`.
- **FR-002**: Utilizar estado local para los campos del formulario.
- **FR-003**: Cargar el `<select>` dinámicamente con `GET_SUBJECTS`.
- **FR-004**: Enviar la mutación `CREATE_INQUIRY` utilizando variables planas sin encapsularlas en un payload.
